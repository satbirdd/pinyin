<p id="notice"><%= notice %></p>
<p>
  <strong>名称:</strong>
  <%= @admin_paper.name %>
</p>

<p>
  <strong>拼音字母:</strong>
  <% @admin_paper.letters.group_by(&:category).each do |category, letters| %>
    <div>
      <%= category %>
      <div>
        <% letters.each do |letter| %>
          <span style="font-size: 26px;"><%= letter.name %></span>
        <% end %>
      </div>
    </div>
  <% end %>
</p>

<%= link_to '编辑', edit_admin_paper_path(@admin_paper) %> |
<%= link_to '返回', admin_papers_path %>
