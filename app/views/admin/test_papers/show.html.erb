<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @admin_test_paper.name %>
</p>

<p>
  <strong>Letters:</strong>
  <% @admin_test_paper.letters.group_by(&:category).each do |category, letters| %>
    <div>
      <%= category %>
      <div>
        <% letters.each do |letter| %>
          <span style="font-size: 26px;"><%= letter.name %></span>
        <% end %>
      </div>
    </div>
  <% end %>
</p>

<%= link_to 'Edit', edit_admin_test_paper_path(@admin_test_paper) %> |
<%= link_to 'Back', admin_test_papers_path %>
