<p id="notice"><%= notice %></p>

<h1>Listing Tests</h1>

<table>
  <thead>
    <tr>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tests.each do |test| %>
      <tr>
        <td><%= link_to 'Show', paper_test_path(@paper, test) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
