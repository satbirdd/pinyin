<p id="notice"><%= notice %></p>

<p>
  <strong>名称:</strong>
  <%= @test.paper.name %>
</p>

<div class='test-area'>
  <button class='btn btn-primary btn-lg begin-test'>开始测试</button>
</div>

<%= link_to '返回', paper_path(@paper) %>

<script type="text/javascript">
  var test = new Test({
    letters: <%= @letters.to_json.html_safe %>,
    urlRoot: "<%= paper_test_path(@paper, @test) %>",
    triggerButton: $('button.begin-test'),
    element: $('div.test-area')
  });
</script>